/* =========================================================
   CLOSING EVENT
   SIMPLE THEATRE EXPERIENCE
   ========================================================= */

document.addEventListener('DOMContentLoaded', () => {
  document.body.classList.add('stage-ready');
});
